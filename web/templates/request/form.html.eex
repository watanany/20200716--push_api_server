<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <div class="form-group">
    <%= label f, :application_id, class: "control-label" %>
    <%= number_input f, :application_id, class: "form-control" %>
    <%= error_tag f, :application_id %>
  </div>

  <div class="form-group">
    <%= label f, :source_ip, class: "control-label" %>
    <%= text_input f, :source_ip, class: "form-control" %>
    <%= error_tag f, :source_ip %>
  </div>

  <div class="form-group">
    <%= submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>
