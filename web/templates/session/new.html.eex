<h2>Sign in</h2>

<%= render "form.html", conn: @conn,
                        changeset: @changeset,
                        action: user_path(@conn, :create),
                        refer: @refer %>

<%= link "Back", to: user_path(@conn, :index) %>
